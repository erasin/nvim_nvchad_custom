local utils = require "core.utils"
local map = utils.map

local M = {}

M.init = function()

  -- vim.g.VM_maps = {}
  -- vim.g.VM_maps['Find Under']         = '<C-d>'           -- replace C-n
  -- vim.g.VM_maps['Find Subword Under'] = '<C-d>'           -- replace visual C-n
  -- vim.g.VM_maps['Add Cursor Down'] = '<D-Down>'      -- start selecting down
  -- vim.g.VM_maps['Add Cursor Up']   = '<D-Up>'        -- start selecting up
  -- if jit.os == "OSX" then
  -- end

  -- vim.g.VM_mouse_mappings = 1
end

return M
